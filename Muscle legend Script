Sure! Let’s add the additional controls for changing player speed and hip height to the GUI, along with organizing them properly. Here’s how you can set it up:

### GUI Layout

1. **Add a Title for Size and Speed**:
   - Add a `TextLabel` at the top of the `ContentFrame` for the title "Size" and "Speed".

2. **Add Size Adjustment Buttons**:
   - Two buttons ("Small" and "Big") for adjusting the player’s size.

3. **Add Speed Adjustment Buttons**:
   - Three buttons ("Slow", "Medium", "Fast") for changing the player's walk speed.

4. **Add Hip Height Slider**:
   - A `Slider` for adjusting the player’s hip height.

### Updated Script

Here’s how to implement these changes with an updated `LocalScript`:

```lua
-- LocalScript placed inside MainFrame

local mainFrame = script.Parent
local toggleButton = mainFrame.Parent:WaitForChild("ToggleButton")
local sidebar = mainFrame:WaitForChild("Sidebar")
local contentFrame = mainFrame:WaitForChild("ContentFrame")

local tabs = sidebar:GetChildren()

-- Function to initialize content for the "Main" tab
local function initializeContent(tabName)
    contentFrame:ClearAllChildren()
    
    if tabName == "Main" then
        -- Title Label
        local titleLabel = Instance.new("TextLabel")
        titleLabel.Size = UDim2.new(1, 0, 0, 50)
        titleLabel.Position = UDim2.new(0, 0, 0, 0)
        titleLabel.Text = "Size"
        titleLabel.TextScaled = true
        titleLabel.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
        titleLabel.Parent = contentFrame

        -- Size Buttons
        local smallButton = Instance.new("TextButton")
        smallButton.Size = UDim2.new(0, 200, 0, 50)
        smallButton.Position = UDim2.new(0.5, -100, 0.1, 0)
        smallButton.Text = "Small"
        smallButton.Parent = contentFrame
        
        local bigButton = Instance.new("TextButton")
        bigButton.Size = UDim2.new(0, 200, 0, 50)
        bigButton.Position = UDim2.new(0.5, -100, 0.2, 0)
        bigButton.Text = "Big"
        bigButton.Parent = contentFrame

        -- Speed Title Label
        local speedTitleLabel = Instance.new("TextLabel")
        speedTitleLabel.Size = UDim2.new(1, 0, 0, 50)
        speedTitleLabel.Position = UDim2.new(0, 0, 0.3, 0)
        speedTitleLabel.Text = "Speed"
        speedTitleLabel.TextScaled = true
        speedTitleLabel.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
        speedTitleLabel.Parent = contentFrame

        -- Speed Buttons
        local slowButton = Instance.new("TextButton")
        slowButton.Size = UDim2.new(0, 200, 0, 50)
        slowButton.Position = UDim2.new(0.5, -100, 0.4, 0)
        slowButton.Text = "Slow"
        slowButton.Parent = contentFrame
        
        local mediumButton = Instance.new("TextButton")
        mediumButton.Size = UDim2.new(0, 200, 0, 50)
        mediumButton.Position = UDim2.new(0.5, -100, 0.5, 0)
        mediumButton.Text = "Medium"
        mediumButton.Parent = contentFrame
        
        local fastButton = Instance.new("TextButton")
        fastButton.Size = UDim2.new(0, 200, 0, 50)
        fastButton.Position = UDim2.new(0.5, -100, 0.6, 0)
        fastButton.Text = "Fast"
        fastButton.Parent = contentFrame

        -- Hip Height Title Label
        local hipHeightTitleLabel = Instance.new("TextLabel")
        hipHeightTitleLabel.Size = UDim2.new(1, 0, 0, 50)
        hipHeightTitleLabel.Position = UDim2.new(0, 0, 0.7, 0)
        hipHeightTitleLabel.Text = "Hip Height"
        hipHeightTitleLabel.TextScaled = true
        hipHeightTitleLabel.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
        hipHeightTitleLabel.Parent = contentFrame

        -- Hip Height Slider
        local slider = Instance.new("Frame")
        slider.Size = UDim2.new(0, 200, 0, 50)
        slider.Position = UDim2.new(0.5, -100, 0.8, 0)
        slider.BackgroundColor3 = Color3.fromRGB(180, 180, 180)
        slider.Parent = contentFrame

        local sliderHandle = Instance.new("Frame")
        sliderHandle.Size = UDim2.new(0, 20, 1, 0)
        sliderHandle.Position = UDim2.new(0, 0, 0, 0)
        sliderHandle.BackgroundColor3 = Color3.fromRGB(100, 100, 255)
        sliderHandle.Parent = slider
        
        local sliderLabel = Instance.new("TextLabel")
        sliderLabel.Size = UDim2.new(1, 0, 1, 0)
        sliderLabel.Text = "Hip Height Value: 2"
        sliderLabel.TextScaled = true
        sliderLabel.BackgroundTransparency = 1
        sliderLabel.Parent = slider

        -- Functions to change player properties
        local function setPlayerSize(size)
            local player = game.Players.LocalPlayer
            local character = player and player.Character
            if character then
                for _, part in ipairs(character:GetChildren()) do
                    if part:IsA("BasePart") then
                        part.Size = Vector3.new(size, size, size)
                    end
                end
            end
        end

        local function setPlayerSpeed(speed)
            local player = game.Players.LocalPlayer
            local character = player and player.Character
            local humanoid = character and character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                humanoid.WalkSpeed = speed
            end
        end

        local function setHipHeight(height)
            local player = game.Players.LocalPlayer
            local character = player and player.Character
            local humanoid = character and character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                humanoid.HipWidth = height
                sliderLabel.Text = "Hip Height Value: " .. height
            end
        end

        -- Connect buttons to change properties
        smallButton.MouseButton1Click:Connect(function()
            setPlayerSize(1)
        end)
        
        bigButton.MouseButton1Click:Connect(function()
            setPlayerSize(100)
        end)
        
        slowButton.MouseButton1Click:Connect(function()
            setPlayerSpeed(16)
        end)
        
        mediumButton.MouseButton1Click:Connect(function()
            setPlayerSpeed(200)
        end)
        
        fastButton.MouseButton1Click:Connect(function()
            setPlayerSpeed(500)
        end)
        
        -- Slider functionality
        local function updateSlider(value)
            sliderHandle.Position = UDim2.new(value / 10, 0, 0, 0)
            setHipHeight(value)
        end

        slider.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 then
                local function onMove(input)
                    local x = input.Position.X - slider.AbsolutePosition.X
                    x = math.clamp(x, 0, slider.AbsoluteSize.X)
                    local value = math.floor((x / slider.AbsoluteSize.X) * 10)
                    updateSlider(value)
                end

                local connection
                connection = input.InputChanged:Connect(onMove)
                input.InputEnded:Connect(function()
                    connection:Disconnect()
                end)
            end
        end)
        
        -- Initialize default slider value
        updateSlider(2)
    elseif tabName == "Teleports" then
        -- Add content for Teleports tab
    elseif tabName == "Auto Farm" then
        -- Add content for Auto Farm tab
    elseif tabName == "Auto Rebirth" then
        -- Add content for Auto Rebirth tab
    elseif tabName == "Auto Kill" then
        -- Add content for Auto Kill tab
    elseif tabName == "View Players Stats" then
        -- Add content for View Players Stats tab
    elseif tabName == "Crystal" then
        -- Add content for Crystal tab
    elseif tabName == "Buy Pets" then
        -- Add content for Buy Pets tab
    elseif tabName == "Apk" then
        -- Add content for Apk tab
    elseif tabName == "Misc" then
        -- Add content for Misc tab
    end
end

-- Function to handle tab switching
local function switchTab(tabName)
    for _, tab in ipairs(tabs) do
        if tab:IsA("TextButton") then
            if tab.Name == tabName then
                tab.BackgroundColor3 = Color3.fromRGB(100, 100, 255)
                tab.TextColor3 = Color3.fromRGB(255, 255, 255)
            else
                tab.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
               
